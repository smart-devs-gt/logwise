version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g typescript
      - npm install
  build:
    commands:
      - npm run build
      - npm pack
artifacts:
  files:
    - "*.tgz"
